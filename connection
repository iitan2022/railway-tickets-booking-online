def connection():
    import mysql.connector
    mycon=mysql.connector.connect(host='localhost',user='root',passwd='7838637874',database='indian_railways')
    if mycon.is_connected():
        print("successfully connected")
connection()
